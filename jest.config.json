{
  "preset": "ts-jest",
  "testEnvironment": "node",
  "modulePathIgnorePatterns": [
    "<rootDir>/dist/",
    "<rootDir>/build/",
    "<rootDir>/template/test"
  ],
  "testPathIgnorePatterns": [
    "<rootDir>/dist/",
    "<rootDir>/build/"
  ],
  "moduleNameMapper": {
    "(.+)\\.js": "$1"
  }
}